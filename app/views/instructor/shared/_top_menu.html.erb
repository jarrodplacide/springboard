<nav class="navbar navbar-default navbar-fixed-top" id="instructor-portal-nav" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navbar-collapse">
        <span class="sr-only">Toggle Navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to(image_tag('small-logo.png', height: 25), '#', class: 'navbar-brand') %>
    </div>

    <div class='collapse navbar-collapse' id='main-navbar-collapse'>
      <ul class="nav navbar-nav">
        <li><%= link_to "Portal Home", instructor_portal_path %></li>
        <li><%= link_to 'My Profile', instructor_show_profile_path %></li>
        <li class="dropdown">
          <%= link_to raw("My Subjects <span class='caret'></span>"), "#", class: 'dropdown-toggle', data: {toggle: 'dropdown'} %>
          <ul class="dropdown-menu" role="menu">
            <% unless @instructor_subjects.empty? %>
                <% @instructor_subjects.each do |section| %>
                    <li><%= link_to section.subject.name + ' - ' + section.codename, instructor_section_path(section) %></li>
                <% end %>
            <% else %>
                <li><%= link_to "You aren't currently assigned to any sections", '#' %></li>
            <% end %>
          </ul>
        </li>
        <li><%= link_to 'My Schedule', instructor_portal_schedule_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <%= link_to raw("My Account <span class='caret'></span>"), "#", class: 'dropdown-toggle', data: {toggle: 'dropdown'} %>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to 'Change Password', '#' %></li>
            <li><%= link_to "Log Out", destroy_instructor_session_path, method: :delete %></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>